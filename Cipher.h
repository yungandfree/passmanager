#pragma once
#include <string>
#include <vector>
#include <array>
#include <algorithm>

class Cipher {
private:
    int Nb = 4;
    int Nk = 4;
    int Nr = 10;

    std::vector<unsigned char> w;

    unsigned char xtimes(unsigned char b) {
        return (b & 0x80) ? ((b << 1) ^ 0x1b) : (b << 1);
    }

    unsigned char multiply(unsigned char a, unsigned char b) {
        unsigned char p = 0;
        for (int i = 0; i < 8; ++i) {
            if (b & 1) p ^= a;
            a = xtimes(a);
            b >>= 1;
        }
        return p;
    }
    
    const unsigned char sbox[256] = {
        
    };

    const unsigned char rsbox[256] = {
        
    };

    const unsigned char rcon[11] = {
        0x00, 0x01, 0x02, 0x04, 0x08, 0x10, 0x20, 0x40, 0x80, 0x1b, 0x36
    };

    void RotWord(std::array<unsigned char, 4>& word) {
        unsigned char tmp = word[0];
        word[0] = word[1];
        word[1] = word[2];
        word[2] = word[3];
        word[3] = tmp;
    }

    void SubWord(std::array<unsigned char, 4>& word) {
        for (int i = 0; i < 4; ++i) word[i] = sbox[word[i]];
    }

    void SubBytes(unsigned char state[4][4]) {
        for (int i = 0; i < 4; ++i)
            for (int j = 0; j < 4; ++j)
                state[i][j] = sbox[state[i][j]];
    }

    void InvSubBytes(unsigned char state[4][4]) {
        for (int i = 0; i < 4; ++i)
            for (int j = 0; j < 4; ++j)
                state[i][j] = rsbox[state[i][j]];
    }

    void ShiftRows(unsigned char state[4][4]) {
        unsigned char temp;
        temp = state[1][0]; state[1][0] = state[1][1]; state[1][1] = state[1][2]; state[1][2] = state[1][3]; state[1][3] = temp;
        temp = state[2][0]; state[2][0] = state[2][2]; state[2][2] = temp; temp = state[2][1]; state[2][1] = state[2][3]; state[2][3] = temp;
        temp = state[3][3]; state[3][3] = state[3][2]; state[3][2] = state[3][1]; state[3][1] = state[3][0]; state[3][0] = temp;
    }

    void InvShiftRows(unsigned char state[4][4]) {
        unsigned char temp;
        temp = state[1][3]; state[1][3] = state[1][2]; state[1][2] = state[1][1]; state[1][1] = state[1][0]; state[1][0] = temp;
        temp = state[2][0]; state[2][0] = state[2][2]; state[2][2] = temp; temp = state[2][1]; state[2][1] = state[2][3]; state[2][3] = temp;
        temp = state[3][0]; state[3][0] = state[3][1]; state[3][1] = state[3][2]; state[3][2] = state[3][3]; state[3][3] = temp;
    }

    void MixColumns(unsigned char state[4][4]) {
        unsigned char tmp[4];
        for (int j = 0; j < 4; ++j) {
            for (int i = 0; i < 4; ++i) tmp[i] = state[i][j];
            state[0][j] = xtimes(tmp[0]) ^ (xtimes(tmp[1]) ^ tmp[1]) ^ tmp[2] ^ tmp[3];
            state[1][j] = tmp[0] ^ xtimes(tmp[1]) ^ (xtimes(tmp[2]) ^ tmp[2]) ^ tmp[3];
            state[2][j] = tmp[0] ^ tmp[1] ^ xtimes(tmp[2]) ^ (xtimes(tmp[3]) ^ tmp[3]);
            state[3][j] = (xtimes(tmp[0]) ^ tmp[0]) ^ tmp[1] ^ tmp[2] ^ xtimes(tmp[3]);
        }
    }

    void InvMixColumns(unsigned char state[4][4]) {
        unsigned char tmp[4];
        for (int j = 0; j < 4; ++j) {
            for (int i = 0; i < 4; ++i) tmp[i] = state[i][j];
            state[0][j] = multiply(tmp[0], 0x0e) ^ multiply(tmp[1], 0x0b) ^ multiply(tmp[2], 0x0d) ^ multiply(tmp[3], 0x09);
            state[1][j] = multiply(tmp[0], 0x09) ^ multiply(tmp[1], 0x0e) ^ multiply(tmp[2], 0x0b) ^ multiply(tmp[3], 0x0d);
            state[2][j] = multiply(tmp[0], 0x0d) ^ multiply(tmp[1], 0x09) ^ multiply(tmp[2], 0x0e) ^ multiply(tmp[3], 0x0b);
            state[3][j] = multiply(tmp[0], 0x0b) ^ multiply(tmp[1], 0x0d) ^ multiply(tmp[2], 0x09) ^ multiply(tmp[3], 0x0e);
        }
    }

    void AddRoundKey(unsigned char state[4][4], int round) {
        for (int c = 0; c < 4; ++c)
            for (int r = 0; r < 4; ++r)
                state[r][c] ^= w[round * 16 + c * 4 + r];
    }

    std::vector<unsigned char> string_to_state(std::string data) {
        int padding = 16 - (data.size() % 16);
        std::vector<unsigned char> storage(data.begin(), data.end());
        storage.insert(storage.end(), padding, static_cast<unsigned char>(padding));
        return storage; 
    }

public: 
    Cipher() {w.resize(176);}

    void KeyExpansion(std::array<unsigned char, 16> key) {
        std::copy(key.begin(), key.end(), w.begin());
        for (int i = Nk; i < 4 * (Nr + 1); ++i) {
            std::array<unsigned char, 4> temp;
            for (int j = 0; j < 4; ++j) temp[j] = w[(i - 1) * 4 + j];
            if (i % Nk == 0) {
                RotWord(temp);
                SubWord(temp);
                temp[0] ^= rcon[i / Nk];
            }
            for (int j = 0; j < 4; ++j) w[i * 4 + j] = w[(i - Nk) * 4 + j] ^ temp[j];
        }
    }

    std::vector<unsigned char> encrypt(std::string data, std::array<unsigned char, 16> key) {
        KeyExpansion(key);
        std::vector<unsigned char> vector_states = string_to_state(data);
        std::vector<unsigned char> result;
        unsigned char state[4][4];

        for (size_t block = 0; block < vector_states.size() / 16; ++block) {
            for (int i = 0; i < 4; ++i)
                for (int j = 0; j < 4; ++j)
                    state[j][i] = vector_states[block * 16 + i * 4 + j];

            AddRoundKey(state, 0);

            for (int round = 1; round < Nr; ++round) {
                SubBytes(state);
                ShiftRows(state);
                MixColumns(state);
                AddRoundKey(state, round);
            }

            SubBytes(state);
            ShiftRows(state);
            AddRoundKey(state, Nr);

            for (int i = 0; i < 4; ++i)
                for (int j = 0; j < 4; ++j)
                    result.push_back(state[j][i]);
        }
        return result;
    }

    std::vector<unsigned char> decrypt(std::vector<unsigned char> encryptedtext, std::array<unsigned char, 16> key) {
        KeyExpansion(key);
        std::vector<unsigned char> result;
        unsigned char state[4][4];

        for (size_t block = 0; block < encryptedtext.size() / 16; ++block) {
            for (int i = 0; i < 4; ++i)
                for (int j = 0; j < 4; ++j)
                    state[j][i] = encryptedtext[block * 16 + i * 4 + j];

            AddRoundKey(state, Nr);

            for (int round = Nr - 1; round > 0; --round) {
                InvShiftRows(state);
                InvSubBytes(state);
                AddRoundKey(state, round);
                InvMixColumns(state);
            }

            InvShiftRows(state);
            InvSubBytes(state);
            AddRoundKey(state, 0);

            for (int i = 0; i < 4; ++i)
                for (int j = 0; j < 4; ++j)
                    result.push_back(state[j][i]);
        }

        if (!result.empty()) {
            unsigned char pad_val = result.back();
            if (pad_val > 0 && pad_val <= 16) result.erase(result.end() - pad_val, result.end());
        }
        return result;
    }
};
